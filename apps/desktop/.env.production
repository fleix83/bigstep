# Produktions-Build (PWA über den Worker, Phase 7): Die API ist same-origin
# (Fallback auf window.location.origin), der Token wird im Settings-Dialog
# eingegeben und in localStorage abgelegt — nichts wird ins Bundle eingebacken.
VITE_API_URL=
VITE_API_TOKEN=
