# Neon Postgres – Connection-String aus der Neon-Konsole (nach Rotation!)
DATABASE_URL=

# Statischer Bearer-Token für die API (frei wählen, z. B. `openssl rand -hex 32`)
API_TOKEN=

# Neon-Branch "test" – nur für API-Tests (apps/api)
TEST_DATABASE_URL=
